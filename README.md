# webcrawlerhttp

## Description
This is a console application that takes a single URL, fetches the HTML content, and parses out all internal links to discover the website's structure.

## Requirements
Node >= 18.20.3

### Get Node Version
Run `nvm Use`

### Installation
To install the appropriate version run `npm install`.

### How to Run
Run `npm start https://www.example.com`
